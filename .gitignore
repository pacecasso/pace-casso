node_modules
.next
out
test-results
playwright-report
blob-report
playwright/.cache
.env
.env.local
*.log

# Agent tool metadata
.claude/

# Local reference screenshots / test images at repo root (kept for
# convenience but not tracked). Image assets under public/ and app/ are
# still committed.
/*.png
/*.jpg
/*.jpeg
/*.jfif
/*.webp
/*.gif

# Local GPS-art proof experiments / generated route previews
/tmp-heart-qa/
/tmp-route-art-benchmark/
/tmp-lattice-validate/
