node_modules
.next
out
test-results
playwright-report
blob-report
playwright/.cache
.env
.env.local
*.log

# Agent tool metadata
.claude/

# Local reference screenshots / test images at repo root (kept for
# convenience but not tracked). Image assets under public/ and app/ are
# still committed.
/*.png
/*.jpg
/*.jpeg
/*.jfif
/*.webp
/*.gif

# Local GPS-art proof experiments / generated route previews
/tmp-*
/scripts/tmp-*
.codex/
.vercel
.env*

# depends on gitignored tmp-heart-qa/ — local experiment, never CI-clean
/scripts/proof-nyc-three-logos.ts
