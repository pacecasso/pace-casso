import fs from "node:fs/promises";
import path from "node:path";
import { createRequire } from "node:module";
const require=createRequire(import.meta.url);
const sharp=require("sharp");
const jiti=require("jiti")(import.meta.url);
const { buildLatticeGraph, haversineMeters }=jiti("../lib/latticeCompiler.ts");
const root=process.cwd();
const inFile=path.join(root,"tmp-logo-proof","strava-single-run-from-block","strava-single-run-visible-connector.gpx");
const outDir=path.join(root,"tmp-logo-proof","strava-clean-rerouted");
function parse(xml){const pts=[];const re=/<trkpt lat="([^"]+)" lon="([^"]+)"/g;let m;while((m=re.exec(xml)))pts.push([+m[1],+m[2]]);return pts;}
function splitParts(ps){return [ps.slice(0,642),ps.slice(642,644),ps.slice(644)];}
function basis(ps){const b=boundsLL(ps),lat=(b.minLat+b.maxLat)/2;return{lat0:b.minLat,lng0:b.minLng,mx:111320*Math.cos(lat*Math.PI/180),my:111320};}
function toXY(p,b){return[(p[1]-b.lng0)*b.mx,(p[0]-b.lat0)*b.my];}
function toLL(q,b){return[b.lat0+q[1]/b.my,b.lng0+q[0]/b.mx];}
function boundsLL(ps){const lats=ps.map(p=>p[0]),lngs=ps.map(p=>p[1]);return{minLat:Math.min(...lats),maxLat:Math.max(...lats),minLng:Math.min(...lngs),maxLng:Math.max(...lngs)}}
function pointSegDist(p,a,b){const vx=b[0]-a[0],vy=b[1]-a[1],wx=p[0]-a[0],wy=p[1]-a[1],l2=vx*vx+vy*vy;if(l2===0)return Math.hypot(wx,wy);const t=Math.max(0,Math.min(1,(wx*vx+wy*vy)/l2));return Math.hypot(p[0]-(a[0]+t*vx),p[1]-(a[1]+t*vy));}
function rdp(ps,tol){if(ps.length<=2)return ps.slice();let md=-1,idx=-1;for(let i=1;i<ps.length-1;i++){const d=pointSegDist(ps[i],ps[0],ps[ps.length-1]);if(d>md){md=d;idx=i;}}if(md<=tol)return[ps[0],ps[ps.length-1]];return[...rdp(ps.slice(0,idx+1),tol).slice(0,-1),...rdp(ps.slice(idx),tol)];}
function nearestNode(graph,p){let best=0,bd=Infinity;for(let i=0;i<graph.nodes.length;i++){const d=haversineMeters(p,graph.nodes[i]);if(d<bd){bd=d;best=i;}}return best;}
function localOfLL(p,b){return toXY(p,b)}
function pointSegDistLL(p,a,c,b){return pointSegDist(localOfLL(p,b),localOfLL(a,b),localOfLL(c,b));}
function shortestHug(graph,from,to,b){if(from===to)return{path:[from],m:0};const A=graph.nodes[from],B=graph.nodes[to];const dist=new Map([[from,0]]),len=new Map([[from,0]]),prev=new Map(),done=new Set();const open=[[haversineMeters(A,B),0,from]];while(open.length){let bi=0;for(let i=1;i<open.length;i++)if(open[i][0]<open[bi][0])bi=i;const[,gc,cur]=open.splice(bi,1)[0];if(cur===to)break;if(done.has(cur))continue;done.add(cur);for(const e of graph.adj.get(cur)??[]){const dev=pointSegDistLL(graph.nodes[e.to],A,B,b);const nd=gc+e.len+dev*1.1;if(nd<(dist.get(e.to)??Infinity)){dist.set(e.to,nd);len.set(e.to,(len.get(cur)??0)+e.len);prev.set(e.to,cur);open.push([nd+haversineMeters(graph.nodes[e.to],B),nd,e.to]);}}}if(!prev.has(to))return null;const path=[];let cur=to;while(cur!==undefined){path.push(cur);cur=prev.get(cur);}path.reverse();return{path,m:len.get(to)??0};}
function append(out,graph,nodePath){for(let i=0;i<nodePath.length;i++){const node=graph.nodes[nodePath[i]];if(i>0){const prev=nodePath[i-1];const entry=(graph.adj.get(prev)??[]).find(e=>e.to===nodePath[i]);if(entry)for(const v of entry.via??[])out.push(v);}const last=out[out.length-1];if(!last||last[0]!==node[0]||last[1]!==node[1])out.push(node);}}
function routeAnchors(graph,anchors,b){const nodes=[];for(const p of anchors){const n=nearestNode(graph,p);if(nodes[nodes.length-1]!==n)nodes.push(n);}const out=[];let total=0,failed=0;if(!nodes.length)return{chain:[],failed:1,total:0};out.push(graph.nodes[nodes[0]]);for(let i=1;i<nodes.length;i++){const leg=shortestHug(graph,nodes[i-1],nodes[i],b);if(!leg){failed++;continue;}append(out,graph,leg.path);total+=leg.m;}return{chain:out,total,failed};}
function km(ps){let t=0;for(let i=1;i<ps.length;i++)t+=haversineMeters(ps[i-1],ps[i]);return t/1000;}
function bounds(ps){const xs=ps.map(p=>p[1]),ys=ps.map(p=>p[0]);return{minX:Math.min(...xs),maxX:Math.max(...xs),minY:Math.min(...ys),maxY:Math.max(...ys)}}
function project(ll,w,h,pad=70){const b=bounds(ll),mx=111320*Math.cos(((b.minY+b.maxY)/2)*Math.PI/180),spanX=Math.max(1,(b.maxX-b.minX)*mx),spanY=Math.max(1,(b.maxY-b.minY)*111320),s=Math.min((w-pad*2)/spanX,(h-pad*2)/spanY),ox=(w-spanX*s)/2,oy=(h-spanY*s)/2;return p=>[ox+(p[1]-b.minX)*mx*s,oy+(b.maxY-p[0])*111320*s]}
function d(ps,pr){return ps.map((p,i)=>{const q=pr(p);return`${i?'L':'M'} ${q[0].toFixed(1)} ${q[1].toFixed(1)}`}).join(' ')}
async function render(parts,file,label){const all=parts.flat(),W=1080,H=830,pr=project(all,W,H);const paths=parts.map((seg,i)=>`<path d="${d(seg,pr)}" fill="none" stroke="${i===1?'#666':i===2?'#ff9b70':'#fc4c02'}" stroke-width="17" stroke-linecap="round" stroke-linejoin="round"/><path d="${d(seg,pr)}" fill="none" stroke="#111" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" opacity=".65"/>`).join('');const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${W}" height="${H}"><rect width="100%" height="100%" fill="#fff"/>${paths}<text x="24" y="38" font-family="Arial" font-size="20" font-weight="700">${label}</text></svg>`;await sharp(Buffer.from(svg)).jpeg({quality:94}).toFile(file)}
function gpx(name,ps){return`<?xml version="1.0" encoding="UTF-8"?>\n<gpx version="1.1" creator="PaceCasso Strava cleaned rerouted" xmlns="http://www.topografix.com/GPX/1/1"><trk><name>${name}</name><trkseg>\n${ps.map(([la,ln])=>`<trkpt lat="${la.toFixed(7)}" lon="${ln.toFixed(7)}"></trkpt>`).join('\n')}\n</trkseg></trk></gpx>\n`}
async function main(){await fs.rm(outDir,{recursive:true,force:true});await fs.mkdir(outDir,{recursive:true});const pts=parse(await fs.readFile(inFile,"utf8"));const [top,conn,low]=splitParts(pts);const b=basis(pts);const graph=buildLatticeGraph(JSON.parse(await fs.readFile(path.join(root,"lib","data","manhattan-lattice.json"),"utf8")));const rows=[];for(const tol of [20,30,42,55,70,90]){const anchorPart=seg=>rdp(seg.map(p=>toXY(p,b)),tol).map(q=>toLL(q,b));const tAnch=anchorPart(top),lAnch=anchorPart(low);const t=routeAnchors(graph,tAnch,b),l=routeAnchors(graph,lAnch,b);const parts=[t.chain,conn,l.chain];const chain=[...t.chain,...conn,...l.chain];const name=`strava-reroute-${String(tol).padStart(2,'0')}`;await render(parts,path.join(outDir,`${name}.jpg`),`${name} tol ${tol}m ${km(chain).toFixed(1)} km fail ${t.failed+l.failed}`);await fs.writeFile(path.join(outDir,`${name}.gpx`),gpx(name,chain));rows.push({name,tol,km:+km(chain).toFixed(2),points:chain.length,failed:t.failed+l.failed,jpg:path.relative(root,path.join(outDir,`${name}.jpg`)).replace(/\\/g,'/')});}
const comps=[];for(const row of rows){const input=await sharp(path.join(root,row.jpg)).resize(540,415,{fit:'contain',background:'#fff'}).jpeg().toBuffer();comps.push({input,left:(comps.length%2)*540,top:Math.floor(comps.length/2)*415});}
await sharp({create:{width:1080,height:Math.ceil(comps.length/2)*415,channels:3,background:'#fff'}}).composite(comps).jpeg({quality:92}).toFile(path.join(outDir,'candidate-sheet.jpg'));await fs.writeFile(path.join(outDir,'summary.json'),JSON.stringify(rows,null,2));console.log(JSON.stringify({sheet:path.relative(root,path.join(outDir,'candidate-sheet.jpg')).replace(/\\/g,'/'),rows},null,2));}
main().catch(e=>{console.error(e);process.exit(1)});