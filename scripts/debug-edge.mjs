import { createRequire } from "node:module";const require=createRequire(import.meta.url);const jiti=require("jiti")(import.meta.url);const {buildGraph}=jiti("./trace-contour.ts");const g=await buildGraph();const checks=[[7136857065,4161568337],[6330061739,5783022063],[7465091022,7465091025]];for(const [a,b] of checks){console.log(a,b,(g.adj.get(a)??[]).some(e=>e.to===b),(g.adj.get(b)??[]).some(e=>e.to===a),g.adj.get(a)?.slice(0,5));}