import fs from "node:fs/promises";
import path from "node:path";
import { createRequire } from "node:module";
const require=createRequire(import.meta.url); const sharp=require("sharp");
const root=process.cwd(); const stamp=new Date().toISOString().replace(/[:.]/g,"-"); const outDir=path.join(root,"tmp-street-fabric-search",stamp);
function arc(cx,cy,rx,ry,a0,a1,n){const pts=[];for(let i=0;i<=n;i++){const t=a0+(a1-a0)*i/n;pts.push([cx+Math.cos(t)*rx,cy+Math.sin(t)*ry]);}return pts;}
function densify(points,max=28){const out=[];for(let i=0;i<points.length;i++){const a=points[i];out.push(a);const b=points[i+1];if(!b)continue;const d=Math.hypot(b[0]-a[0],b[1]-a[1]);const steps=Math.max(1,Math.ceil(d/max));for(let s=1;s<steps;s++){const t=s/steps;out.push([a[0]+(b[0]-a[0])*t,a[1]+(b[1]-a[1])*t]);}}return out;}
function sneaker(){const outline=[[0,230],[160,140],[430,105],[760,112],[1100,155],[1420,235],...arc(1490,330,150,105,-0.7,1.2,12),[1280,500],[990,555],[760,660],[565,850],[390,800],[285,550],[120,370],[0,230]];const swoosh=[[335,305],[560,220],[930,250],[1325,430],[885,350],[575,360],[335,305]];const laces=[[610,640],[650,455],[735,655],[805,455],[890,625],[965,470],[1040,590]];const heel=[[285,550],[210,785],[390,800]];const tread=[[220,140],[210,60],[350,112],[455,55],[595,112],[705,60],[845,120],[965,75],[1110,155]];return densify([...outline,...swoosh,...laces,...heel,...tread]);}
function witch(){const hat=[[465,500],[675,0],[910,515],[760,430],[675,0]];const brim=[[210,535],[520,440],[920,475],[1170,610],[760,620],[340,590],[210,535]];const face=[[760,620],[835,805],[725,930],[585,875],[520,620]];const nose=[[735,780],[980,835],[755,860]];const cloak=[[585,875],[455,1240],[690,1160],[835,805]];const broom=[[360,1120],[760,1000],[1190,875],[1580,720]];const bristles=[[1580,720],[1400,600],[1660,690],[1430,800],[1710,850],[1430,800],[1640,980]];return densify([...hat,...brim,...face,...nose,...cloak,...broom,...bristles]);}
function bounds(pts){const xs=pts.map(p=>p[0]),ys=pts.map(p=>p[1]);return{minX:Math.min(...xs),maxX:Math.max(...xs),minY:Math.min(...ys),maxY:Math.max(...ys)}}
function project(pts,w,h,pad=50){const b=bounds(pts),s=Math.min((w-pad*2)/(b.maxX-b.minX),(h-pad*2)/(b.maxY-b.minY)),uw=(b.maxX-b.minX)*s,uh=(b.maxY-b.minY)*s,ox=(w-uw)/2,oy=(h-uh)/2;return ([x,y])=>[ox+(x-b.minX)*s,oy+(b.maxY-y)*s];}
function pathD(pts,pr){return pts.map((p,i)=>{const [x,y]=pr(p);return `${i?'L':'M'} ${x.toFixed(1)} ${y.toFixed(1)}`}).join(' ')}
function fabricLines(w,h,step=70){const lines=[];for(let x=40;x<w;x+=step)lines.push(`<path d="M ${x} 30 L ${x} ${h-30}" stroke="#dedede" stroke-width="2"/>`);for(let y=40;y<h;y+=step)lines.push(`<path d="M 30 ${y} L ${w-30} ${y}" stroke="#dedede" stroke-width="2"/>`);for(let x=-h;x<w+h;x+=step*2)lines.push(`<path d="M ${x} ${h-30} L ${x+h-60} 30" stroke="#e6e6e6" stroke-width="1.6"/>`);return lines.join('\n');}
async function render(id,pts,ref){const dir=path.join(outDir,id);await fs.mkdir(dir,{recursive:true});await fs.copyFile(path.join(root,ref),path.join(dir,'source'+path.extname(ref)));const w=1100,h=820,pr=project(pts,w,h),d=pathD(pts,pr);const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}"><rect width="100%" height="100%" fill="#f6f4ef"/><rect x="24" y="24" width="${w-48}" height="${h-48}" fill="#fff"/>${fabricLines(w,h)}<path d="${d}" fill="none" stroke="#7b1024" stroke-width="13" stroke-linecap="round" stroke-linejoin="round"/><path d="${d}" fill="none" stroke="#ef1744" stroke-width="6.5" stroke-linecap="round" stroke-linejoin="round"/></svg>`;await sharp(Buffer.from(svg)).png().toFile(path.join(dir,'route-blind.png'));const refbuf=await sharp(path.join(root,ref)).resize(700,560,{fit:'contain',background:'#fff'}).png().toBuffer();const routebuf=await sharp(path.join(dir,'route-blind.png')).resize(700,560,{fit:'contain',background:'#fff'}).png().toBuffer();const base=Buffer.from(`<svg xmlns="http://www.w3.org/2000/svg" width="1500" height="720"><rect width="100%" height="100%" fill="#f8f5ef"/><text x="40" y="680" font-family="Arial" font-size="16" fill="#555">source sample</text><text x="790" y="680" font-family="Arial" font-size="16" fill="#555">NEW fabric-search route (not yet real-city matched)</text></svg>`);await sharp(base).composite([{input:refbuf,left:40,top:70},{input:routebuf,left:790,top:70}]).png().toFile(path.join(dir,'source-vs-fabric-route.png'));return {id,blindImage:path.relative(root,path.join(dir,'route-blind.png')).replace(/\\/g,'/'),comparisonImage:path.relative(root,path.join(dir,'source-vs-fabric-route.png')).replace(/\\/g,'/')};}
async function main(){await fs.mkdir(outDir,{recursive:true});const summary=[];summary.push(await render('sneaker-fabric',sneaker(),'sneaker.jpg'));summary.push(await render('witch-fabric',witch(),'witch.jpg'));await fs.writeFile(path.join(outDir,'summary.json'),JSON.stringify(summary,null,2));console.log(path.relative(root,outDir));}
main().catch(e=>{console.error(e);process.exit(1)});